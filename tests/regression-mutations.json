{
  "$comment": [
    "REGISTERED MUTATIONS — the evidence behind every @regression test in tests/**.",
    "",
    "WHY: two regression tests written during the 11.33.1 session passed with the bug fully",
    "restored. They were vacuous, and only a hand-run mutation caught that. A regression test that",
    "was never seen failing is a claim, not a check — so the claim is written down here in a form a",
    "machine can re-run: `pnpm run check:mutations` applies each mutation, runs the specs that claim",
    "to catch it, and REQUIRES them to go red.",
    "",
    "Not part of `pnpm run check` (it deliberately breaks the source and re-runs suites; it belongs",
    "in review and on the publish path, not in every inner loop). It is kept honest without running,",
    "though: tests/unit/regression-evidence.spec.ts asserts that every `find` still matches its",
    "target EXACTLY ONCE, so a mutation that has rotted into a silent no-op fails the normal suite.",
    "",
    "FIELDS",
    "  id       referenced from the @seen-failing line of the tests it belongs to",
    "  file     path relative to the repository root",
    "  after    optional anchor; the search starts here (disambiguates identical lines in one file)",
    "  find     literal text, must occur exactly once in the search window",
    "  replace  what it becomes — the defect, reconstructed",
    "  specs    the spec files that MUST fail once the mutation is applied",
    "  driverSpecific  true when only some storage drivers are affected; `drivers` names them"
  ],
  "mutations": [
    {
      "id": "brevo-exclude-stateful-regex",
      "defect": "11.36.0 — the exclude guard called `.test()` on the CONFIGURED RegExp. Projects declare `brevo.exclude` with the `g` flag (`/@(testuser\\.com|test\\.de)/gi`), and `RegExp.prototype.test` advances `lastIndex` on a `g`/`y` pattern, so one shared instance answered true, false, true, … for the same address. Every SECOND excluded recipient was therefore sent a real, billable transactional mail. Found in production: a CI e2e run delivered five mails to a `@testuser.com` address the project's own exclude pattern lists, and Brevo flagged the runner's IP rather than the project noticing.",
      "file": "src/core/common/services/brevo.service.ts",
      "after": "protected isExcluded(to: string): boolean {",
      "find": "    const stateless =\n      exclude.global || exclude.sticky ? new RegExp(exclude.source, exclude.flags.replace(/[gy]/g, '')) : exclude;\n\n    return stateless.test(to);",
      "replace": "    return exclude.test(to);",
      "specs": [
        "tests/unit/brevo.service.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "restriction-flag-migration-warning-silent",
      "defect": "11.35.0 — tightening the `_objectAlreadyCheckedForRestrictions` opt-out is the safe direction but NOT a silent one: a project that set the flag by hand loses its exemption and the fields it set the flag to preserve are stripped. Three customer projects do exactly that across twelve call sites. Without the one-time warning naming markRestrictionsChecked(), they would debug stripped fields with nothing pointing at the cause.",
      "file": "src/core/common/decorators/restricted.decorator.ts",
      "find": "  warnUnrecognizedRestrictionsFlag(data);\n",
      "replace": "",
      "specs": [
        "tests/unit/restriction-bypass-flag.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "file-access-preset-not-reached",
      "defect": "Not a shipped defect — a NEW feature, and this proves its wiring is not vacuous. `file.access` is only worth anything if CoreFileService.checkRights() actually consults the preset; a base implementation that still answered `true` would leave every preset a decoration, and every test that asserts a refusal would go green for the wrong reason.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "private async resolveAccessPreset(",
      "find": "    if (preset === 'custom') {\n      return true;\n    }",
      "replace": "    if (preset === 'custom' || preset) {\n      return true;\n    }",
      "specs": [
        "tests/unit/file-access-wiring.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "file-access-stamps-no-metadata",
      "defect": "Not a shipped defect — a NEW feature. The 'owner' / 'tenant' presets DECIDE on metadata.ownerId / metadata.tenantId, so the service has to STAMP them as it writes. Without that the preset is a rule about data that does not exist and every file becomes ADMIN-only — which is exactly the shape TUS uploads had before 11.35.0, and it was reported from downstream rather than caught by a test.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "protected accessMetadata(serviceOptions?: FileServiceOptions)",
      "find": "    if (preset !== 'owner' && preset !== 'tenant') {\n      return serviceOptions?.metadata;\n    }",
      "replace": "    if (preset !== 'owner' && preset !== 'tenant' && !preset) {\n      return serviceOptions?.metadata;\n    }\n    return serviceOptions?.metadata;",
      "specs": [
        "tests/unit/file-access-wiring.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "undecided-file-access-warns-silently",
      "defect": "11.35.0 — the boot warning for an open file gate with no per-file policy fired only when multiTenancy was active. Too narrow: `downloadRoles: [S_USER]` with no rule leaks every file to every signed-in user whether or not tenants exist, and file ids are enumerable. The mutation restores the multiTenancy gate.",
      "file": "src/core/modules/file/file-roles.config.ts",
      "after": "export function warnOnUndecidedFileAccess(options: {",
      "find": "  // (1) and (2): somebody decided.\n  if (hasDeclaredFilePolicy({ fileConfig, hasPerFileRule })) {",
      "replace": "  // (1) and (2): somebody decided.\n  if (!multiTenancyEnabled || hasDeclaredFilePolicy({ fileConfig, hasPerFileRule })) {",
      "specs": [
        "tests/unit/file-access-warning.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "presigned-ignores-enabled-false",
      "defect": "11.35.0 — CoreS3Service read ANY object as 'presigned downloads enabled', so the repo-wide `{ enabled: false }` pre-configuration idiom silently turned the feature ON. Reachable without TypeScript via NEST_SERVER_CONFIG / NSC__* JSON. The boot warning for this very feature DID honour the key, so the two paths disagreed and the disagreement resolved towards handing out session-less bearer URLs.",
      "file": "src/core/common/services/core-s3.service.ts",
      "after": "const rawPresigned = raw.presignedDownloads;",
      "find": "    const presigned =\n      rawPresigned && typeof rawPresigned === 'object' && (rawPresigned as { enabled?: boolean }).enabled === false\n        ? undefined\n        : rawPresigned;",
      "replace": "    const presigned = rawPresigned;",
      "specs": [
        "tests/unit/s3-enabled-contract.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reference-rule-allows-unrestricted-listing",
      "defect": "11.35.0 — the REFERENCE checkRights() (src/server/modules/file/file.service.ts, the shape every project copies) narrowed only 'id' and 'filename' and returned true for 'filterArgs'. A project surfacing findFileInfo() therefore exposed a full inventory of every upload — CoreFileInfo carries filename, length, uploadDate and the id, and for medical data the filename frequently IS the content. A yes/no hook cannot narrow a listing, so the rule must refuse the unrestricted one; a per-user listing is expressed by FORCING the filter server-side.",
      "file": "src/server/modules/file/file.service.ts",
      "after": "protected override async checkRights(",
      "find": "    if (options?.force || options?.checkInputType === 'file' || options?.checkInputType === 'files') {",
      "replace": "    if (options?.force || (options?.checkInputType !== 'filename' && options?.checkInputType !== 'id')) {",
      "specs": [
        "tests/file-ownership.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "create-file-skips-write-gate",
      "defect": "Not a shipped defect — this pins a gate that was correct but UNTESTED. `createFile()`'s own `checkInputType: 'file'` check was exercised by nothing: the canonical ownership rule the matrix installs returns true for every input type other than id/filename, so an upload refusal (quota, mime, tenant) was never verified to answer null or to leave no orphan bytes behind. The mutation removes the gate.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "async createFile(file: MaybePromise<FileUploadSource>, serviceOptions?: FileServiceOptions)",
      "find": "    if (!(await this.checkRights(file, { ...serviceOptions, checkInputType: 'file' }))) {\n      return null;\n    }",
      "replace": "",
      "specs": [
        "tests/file-storage-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "restriction-bypass-flag-trusts-data",
      "defect": "11.35.0 — the `_objectAlreadyCheckedForRestrictions` opt-out was a truthy test on a well-known property name, and nothing in src/ ever wrote it. So a document carrying `_objectAlreadyCheckedForRestrictions: true` (raw write, strict:false schema, restored export) disabled EVERY field-level @Restricted check for that object — silently, on the output path where a skipped check produces no error.",
      "file": "src/core/common/decorators/restrictions-checked.marker.ts",
      "after": "export function hasRestrictionsCheckedMarker(data: unknown): boolean {",
      "find": "(data as Record<string, unknown>)[RESTRICTIONS_CHECKED_KEY] === MARKER;",
      "replace": "!!(data as Record<string, unknown>)[RESTRICTIONS_CHECKED_KEY];",
      "specs": [
        "tests/unit/restriction-bypass-flag.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "tenant-guard-blind-on-websocket",
      "defect": "11.35.0 — every role guard resolved the request as `ctx.getContext()?.req || switchToHttp().getRequest()`, which yields nothing for a GraphQL subscription (the WS context has no `req`, and the HTTP fallback on a GraphQL context returns the resolver root). CoreTenantGuard then hit `if (!request) return true` and GRANTED access without deciding anything — and because the role guard delegates non-system roles to it when multi-tenancy is active, `@Roles()` on a subscription was checked by nobody: a plain `member` reached a tenantAdmin-gated subscription and received its messages.",
      "file": "src/core/common/helpers/execution-context-request.helper.ts",
      "after": "export function resolveGuardRequest(",
      "find": "  if (isSubscriptionContext(gqlContext)) {",
      "replace": "  if (false && isSubscriptionContext(gqlContext)) {",
      "specs": [
        "tests/tenant-context-surfaces.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "tus-upload-records-no-owner",
      "defect": "11.35.0 — a finished tus upload's file metadata carried no owner, so the per-file ownership rule CoreFileService.checkRights() documents (metadata.ownerId === currentUser.id) could never authorize a tus-uploaded file: it failed closed for everyone but ADMIN.",
      "file": "src/core/modules/tus/core-tus.service.ts",
      "after": "const fileMetadata = {",
      "find": "        ...(metadata[TUS_OWNER_METADATA_KEY] ? { ownerId: metadata[TUS_OWNER_METADATA_KEY] } : {}),\n",
      "replace": "",
      "specs": [
        "tests/tus-ownership.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "tus-any-user-touches-any-upload",
      "defect": "11.35.0 — HEAD / PATCH / DELETE on a tus upload carried only the coarse tus.roles gate, so any authenticated caller who learned an upload id could read its offset, APPEND BYTES to somebody else's upload (whose content is then migrated into the file store under the victim's filename), or terminate it.",
      "file": "src/core/modules/tus/core-tus.service.ts",
      "after": "protected async assertUploadOwnership(req: any, uploadId: string): Promise<void> {",
      "find": "    if (!uploadId) {\n      return;\n    }",
      "replace": "    if (uploadId || !uploadId) {\n      return;\n    }",
      "specs": [
        "tests/tus-ownership.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "ws-subscribe-without-request-context",
      "defect": "11.35.0 — GraphQL operations arriving over a WebSocket ran with NO RequestContext at all (no Express middleware runs on an upgrade, and CoreTenantGuard finds no `req` on a subscription context). mongooseTenantPlugin reads 'no context' as 'system operation, no filter', so a tenant-scoped read while delivering a subscription message returned EVERY tenant's rows and the plugin's safety net could not notice.",
      "file": "src/core/common/helpers/graphql-ws-context.helper.ts",
      "after": "export function createRequestContextAwareSubscribe(subscribe: ExecuteFn): ExecuteFn {",
      "find": "    const result = await RequestContext.run(context, () => subscribe(args));\n    return isAsyncIterable(result) ? withRequestContextAsyncIterator(result as AsyncIterator<any>, context) : result;",
      "replace": "    return subscribe(args);",
      "specs": [
        "tests/tenant-context-surfaces.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "exclude-schemas-warns-silently",
      "defect": "11.35.0 — a schema that DECLARES tenantId and is listed in multiTenancy.excludeSchemas lost its tenant filter with no signal anywhere. The framework's own docs carried `excludeSchemas: ['User', 'Session']` in a copyable @example, which on a project with per-tenant users switches isolation off for the user collection.",
      "file": "src/core/common/plugins/mongoose-tenant.plugin.ts",
      "after": "function shouldBypass(modelName?: string): boolean {",
      "find": "    warnIsolationDisabled(modelName);\n",
      "replace": "",
      "specs": [
        "tests/tenant-context-surfaces.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "nested-restricted-ignores-declared-type",
      "defect": "11.35.0 — checkRestricted() read restriction metadata off the VALUE's constructor only. An embedded subdocument read out of MongoDB is a plain object whose constructor is Object, which carries no @Restricted metadata, so every nested restriction evaluated to 'no restrictions' and the field was returned in full to callers who may not see it.",
      "file": "src/core/common/decorators/restricted.decorator.ts",
      "after": "const metadataOwner: any =",
      "find": "      : typeof declaredType === 'function'\n        ? declaredType\n        : undefined;",
      "replace": "      : undefined;",
      "specs": [
        "tests/unit/restricted-nested.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "by-name-lookup-unsorted",
      "defect": "11.35.0 — GridFSHelper.findFileByName() read natural order (the OLDEST file of a reused name) while openDownloadStreamByName() defaults to revision -1 (the NEWEST). So getFileInfoByName()/getRawFileInfoByName() authorized against one document and getFileStreamByName()/getBufferByName()/duplicateByName() served another — an ownership rule approved the caller's own file and handed over somebody else's bytes.",
      "file": "src/core/common/helpers/gridfs.helper.ts",
      "after": "static async findFileByName(bucket: GridFSBucket, filename: string)",
      "find": "bucket.find({ filename }, { limit: 1, sort: { uploadDate: -1, _id: -1 } })",
      "replace": "bucket.find({ filename })",
      "specs": [
        "tests/file-storage-parity.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "gridfs"
      ]
    },
    {
      "id": "delete-by-name-drops-context",
      "defect": "11.33.1 — deleteFileByName() authorized the caller and then re-resolved the file with an EMPTY context, so an overridden checkRights() was asked two different questions about one request and the owner was told `File not found`.",
      "file": "src/core/modules/file/core-file.service.ts",
      "find": "getFileInfoByName(filename, serviceOptions)",
      "replace": "getFileInfoByName(filename)",
      "specs": [
        "tests/file-storage-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "raw-by-name-skips-filesystem",
      "defect": "11.33.0 — getRawFileInfoByName() consulted S3 and GridFS but not the filesystem store, so a by-name ownership rule authorized against a different file set than the download route served.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "protected async getRawFileInfoByName(filename: string)",
      "find": "      (await this.findFilesystemFileByName(filename)) ||\n",
      "replace": "",
      "specs": [
        "tests/file-storage-parity.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "filesystem"
      ]
    },
    {
      "id": "s3-lookup-gated-on-active-driver",
      "defect": "11.33.0 — the S3 metadata lookups were gated on the ACTIVE driver instead of on whether S3 is usable, so adopting S3 kept GridFS readable while switching back turned every S3-stored file into a 404.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "protected async findS3FileById(id: string | Types.ObjectId)",
      "find": "if (!this.isStorageAvailable('s3')) {\n      return null;\n    }\n    return S3FileHelper.findFileById(",
      "replace": "if (this.storageDriver !== 's3') {\n      return null;\n    }\n    return S3FileHelper.findFileById(",
      "specs": [
        "tests/file-storage-parity.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "filesystem",
        "gridfs"
      ]
    },
    {
      "id": "duplicate-by-name-drops-context",
      "defect": "11.33.1 — duplicateByName() accepted no serviceOptions at all and re-resolved the source with an EMPTY context, so under a fail-closed checkRights() the owner's own duplicate was refused (and on the GridFS driver the method bypassed checkRights() entirely and copied unchecked).",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "async duplicateByName(name: string, newName: string",
      "find": "const source = await this.getFileInfoByName(name, serviceOptions);",
      "replace": "const source = await this.getFileInfoByName(name);",
      "specs": [
        "tests/file-duplicate.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "duplicate-by-id-null-deref",
      "defect": "11.33.1 — duplicateById() re-resolved the source with an EMPTY context and then dereferenced the null it got back (`file.filename`), so a fail-closed checkRights() produced a TypeError where a 404 belongs — under every storage driver.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "async duplicateById(id: string | Types.ObjectId",
      "find": "const source = await this.getFileInfo(objectId, serviceOptions);\n    if (!source) {",
      "replace": "const source = await this.getFileInfo(objectId);\n    if (source === undefined) {",
      "specs": [
        "tests/file-duplicate.e2e-spec.ts",
        "tests/file-missing-answers.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "delete-by-id-500-instead-of-404",
      "defect": "11.33.1 — deleteFile() fell through to the GridFS driver for an unknown id and surfaced its MongoRuntimeError ('File not found for id …') as an HTTP 500, while its by-name sibling answered a clean 404 for the same condition.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "async deleteFile(id: string | Types.ObjectId",
      "find": "    if (!fileInfo) {\n      throw new NotFoundException(`File not found with id ${getStringIds(objectId)}`);\n    }\n",
      "replace": "",
      "specs": [
        "tests/file-missing-answers.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "merged-sort-ignores-dotted-path",
      "defect": "11.33.1 — sortMergedFileInfo() read a sort key with `doc[field]`, so a DOTTED path (metadata.ownerId) was undefined for every row: each store sorted correctly and the merge then tied everything, leaving the page grouped by store. Paging over that returns the WRONG ROWS, not merely the wrong order.",
      "file": "src/core/modules/file/core-file.service.ts",
      "after": "protected sortMergedFileInfo",
      "find": "const left = this.readSortField(a, field);\n        const right = this.readSortField(b, field);",
      "replace": "const left = a?.[field];\n        const right = b?.[field];",
      "specs": [
        "tests/file-find-info.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "filesystem",
        "s3"
      ]
    },
    {
      "id": "gridfs-content-type-not-mapped",
      "defect": "11.33.1 — GridFS keeps contentType inside `metadata` while s3-files / filesystem-files carry it at the document root, so findFileInfo() filtered on contentType matched every S3/filesystem file and NO GridFS file: nothing at all on the default driver, and every pre-switch file silently dropped on any other.",
      "file": "src/core/common/helpers/gridfs.helper.ts",
      "after": "static async findFiles(",
      "find": "const query = GridFSHelper.mapContentTypeKeys(filter);",
      "replace": "const query = filter;",
      "specs": [
        "tests/file-find-info.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "gridfs-upload-source-error-unhandled",
      "defect": "11.33.1 — writeFileFromStream() listened for errors on the DESTINATION stream only. pipe() does not forward source errors, so an aborted upload emitted an unhandled 'error' — an uncaught exception that ends the PROCESS. The same abort was a rejected promise under the S3 and filesystem drivers.",
      "file": "src/core/common/helpers/gridfs.helper.ts",
      "after": "static writeFileFromStream(",
      "find": "      stream.on('error', (error) => {\n        // Discard the chunks already written — see the note above.\n        Promise.resolve(uploadStream.abort?.()).catch(() => undefined);\n        fail(error);\n      });\n\n",
      "replace": "",
      "specs": [
        "tests/file-upload-stream-error.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "gridfs"
      ]
    },
    {
      "id": "tus-staged-objects-not-deleted",
      "defect": "11.33.x — a COMPLETED tus upload's staged object is not an incomplete multipart, so no bucket lifecycle rule reclaims it. Without the explicit cleanup every finished upload leaves a full second copy of itself behind, in a bucket that by default IS the file bucket.",
      "file": "src/core/modules/tus/core-tus.service.ts",
      "after": "protected async deleteStagedObjects(uploadId: string)",
      "find": "    const { stagingBucket } = s3Service.getConfig();\n",
      "replace": "    const { stagingBucket } = s3Service.getConfig();\n    if (stagingBucket) {\n      return;\n    }\n",
      "specs": [
        "tests/tus-finalization.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "s3-body-stream-error-unguarded",
      "defect": "11.33.1 — CoreS3Service.putObject() handed a known-length body stream straight to the AWS SDK, which pipes it into its HTTP request without listening on the SOURCE. A failing body was therefore an unhandled 'error' event — an uncaught exception that ends the process. Reachable from tus finalization into S3, the one upload path that does not buffer first.",
      "file": "src/core/common/services/core-s3.service.ts",
      "after": "protected guardBodyStream(stream: Readable)",
      "find": "    const relay = new PassThrough();\n    let sourceError: Error | undefined;\n\n    stream.on('error', (error: Error) => {\n      sourceError = error;\n      // GRACEFUL end, never `destroy(error)`: propagating the error into the relay would hand the\n      // SDK the failing stream this method exists to keep away from it.\n      relay.end();\n    });\n    stream.pipe(relay);\n\n    return { body: relay, sourceError: () => sourceError };",
      "replace": "    return { body: stream, sourceError: () => undefined };",
      "specs": [
        "tests/file-upload-stream-error.e2e-spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "s3"
      ]
    },
    {
      "id": "hub-files-gridfs-only",
      "defect": "11.33.x — the Hub's Files panel read `fs.files` alone, so under `file.storage: 's3'` or 'filesystem' it reported 0 files and could not delete anything. In an operator cockpit a confident wrong answer is worse than an honest gap: it is the tool somebody opens BECAUSE they are unsure whether an upload landed.",
      "file": "src/core/modules/hub/services/core-hub-db.service.ts",
      "after": "protected fileStores(bucket: string)",
      "find": "      { collection: S3_FILES_COLLECTION, store: 's3' },\n      { collection: FILESYSTEM_FILES_COLLECTION, store: 'filesystem' },\n",
      "replace": "",
      "specs": [
        "tests/unit/core-hub-db.service.spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "filesystem",
        "s3"
      ]
    },
    {
      "id": "hub-s3-delete-orphans-metadata",
      "defect": "11.34.0 — an S3-backed delete must REFUSE when CoreS3Service is unavailable. Deleting the metadata document while the object stays in the bucket produces an orphan nothing can find again — unrecoverable in a way that 'try again with S3 configured' is not.",
      "file": "src/core/modules/hub/services/core-hub-db.service.ts",
      "after": "      case 's3': {",
      "find": "        if (!this.s3Service?.enabled) {\n          throw new Error(HubActionMessage.s3Unavailable);\n        }\n",
      "replace": "",
      "specs": [
        "tests/unit/core-hub-db.service.spec.ts"
      ],
      "driverSpecific": true,
      "drivers": [
        "s3"
      ]
    },
    {
      "id": "hub-files-merge-not-sorted",
      "defect": "11.34.0 — the Files panel merges three stores, so the union must be re-sorted before the page is cut. Without it the listing comes back GROUPED BY STORE, and `skip`/`limit` over that returns the wrong ROWS — the same defect sortMergedFileInfo() was fixed for, one layer up.",
      "file": "src/core/modules/hub/services/core-hub-db.service.ts",
      "find": "    rows.sort((a, b) => (b.uploadDate ?? '').localeCompare(a.uploadDate ?? ''));\n",
      "replace": "",
      "specs": [
        "tests/unit/core-hub-db.service.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "validation-ignores-custom-message",
      "defect": "11.34.1 — MapAndValidatePipe built every message from constraintInstance.defaultMessage() alone, so a custom `message` in ValidationOptions (IsIn([...], { message: '...' })) was silently dropped and the client saw the generic default instead.",
      "file": "src/core/common/pipes/map-and-validate.pipe.ts",
      "after": "const constraintName = metadata.name || 'customValidation';",
      "find": "                      let messageTemplate: string | ((args: ValidationArguments) => string) | undefined =\n                        metadata.message as string | ((args: ValidationArguments) => string) | undefined;\n",
      "replace": "                      let messageTemplate: string | ((args: ValidationArguments) => string) | undefined;\n",
      "specs": [
        "tests/map-and-validate.pipe.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "validation-message-placeholders-raw",
      "defect": "11.34.1 — only $property was substituted, so built-in default messages reached API clients with raw placeholders: \"each value in roles must be one of the following values: $constraint1\".",
      "file": "src/core/common/helpers/validation-message.helper.ts",
      "find": "if (messageString && Array.isArray(validationArguments.constraints)) {",
      "replace": "if (false && messageString && Array.isArray(validationArguments.constraints)) {",
      "specs": [
        "tests/map-and-validate.pipe.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "validation-isdefined-ignores-custom-message",
      "defect": "11.34.1 — the same custom-message gap existed on the non-customValidation path, which since class-validator 0.13 is reached by @IsDefined alone: its hardcoded message won over the message the developer wrote.",
      "file": "src/core/common/pipes/map-and-validate.pipe.ts",
      "after": "          // Add constraint violation if validation failed",
      "find": "            // A custom message from ValidationOptions takes precedence over the built-in message\n            if (metadata.message) {\n              errorMessage = replaceMessageSpecialTokens(\n                metadata.message as string | ((args: ValidationArguments) => string),\n                {\n                  constraints: metadata.constraints || [],\n                  object: tempInstance,\n                  property: propertyName,\n                  targetName: targetClass.name,\n                  value: propertyValue,\n                },\n              );\n            }\n",
      "replace": "",
      "specs": [
        "tests/map-and-validate.pipe.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "validation-mirror-drops-target-token",
      "defect": "11.34.1 — the message renderer is a port of class-validator internals, and a port drifts silently: vendor-mode consumers resolve their OWN class-validator, so src/core/ can meet a version this repo never installed. Dropping $target is the smallest such drift, and no e2e case reaches that token — only the mirror invariant test does.",
      "file": "src/core/common/helpers/validation-message.helper.ts",
      "find": "  if (messageString) {\n    messageString = messageString.replace(/\\$target/g, validationArguments.targetName);\n  }\n",
      "replace": "",
      "specs": [
        "tests/unit/validation-message-mirror.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "user-roles-accepts-system-roles",
      "defect": "11.35.0 — CoreUserInput.roles had no content validator, so an admin could store a system role (s_*) in user.roles. hasRole() is a plain string intersection, so a stored 's_self' satisfied every S_SELF check on ARBITRARY users — update/delete of any account (mail/password change → takeover) without the account carrying a real role. Neutralizing the lookahead mutates the guard's DECISION rather than the existence of validation, so a red result is about this behaviour and not about the pipe having stopped running.",
      "file": "src/core/common/enums/role.enum.ts",
      "find": "export const SYSTEM_ROLE_REJECT_PATTERN = new RegExp(`^(?!\\\\s*${SYSTEM_ROLE_PREFIX})`, 'i');",
      "replace": "export const SYSTEM_ROLE_REJECT_PATTERN = new RegExp('^(?!zz_never_matches_)', 'i');",
      "specs": [
        "tests/server.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "non-dto-write-paths-accept-system-roles",
      "defect": "11.35.0 — the DTO validator only covers the input path. CoreUserService.setRoles() writes straight through findByIdAndUpdate, and force:true / bypassRoleGuard / direct Model writes never reach a pipe at all, so a system role could still be stored through any of them. Both remaining layers (setRoles' explicit check and the unconditional mongooseSystemRolePlugin) derive their decision from looksLikeSystemRole, so neutralizing it is what proves the non-DTO paths are actually guarded. NOTE: the two layers are DELIBERATELY redundant — mutating only setRoles' check leaves the plugin backstop in place and the specs stay green, which is the design working, not a gap. That is why the mutation targets the shared predicate rather than either call site.",
      "file": "src/core/common/enums/role.enum.ts",
      "find": "  return typeof role === 'string' && role.trim().toLowerCase().startsWith(SYSTEM_ROLE_PREFIX);",
      "replace": "  return typeof role === 'string' && role.trim().toLowerCase().startsWith('zz_never_matches_');",
      "specs": [
        "tests/server.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "tenant-role-grants-global-admin",
      "defect": "11.35.0 — in tenant context the guard compared required roles against membership.role by plain string equality, so a member whose tenant role was literally 'admin' satisfied @Roles(RoleEnum.ADMIN), the GLOBAL platform role. Membership roles are customer-assigned free text (addMember takes any non-empty string), so any tenant owner could mint platform-wide access. Collapsing the global/tenant split restores it.",
      "file": "src/core/modules/tenant/core-tenant.helpers.ts",
      "find": "    (scope === RoleScope.GLOBAL || isGlobalOnlyRole(role) ? global : tenant).push(role);",
      "replace": "    (scope === RoleScope.SYSTEM ? global : tenant).push(role);",
      "specs": [
        "tests/tenant-guard.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "restricted-decorator-accepts-system-tenant-role",
      "defect": "11.35.0 — checkRestricted passed the UNFILTERED required-roles list into checkRoleAccess, where a non-hierarchy role matches by exact string against the single available role. In tenant context that is membership.role, so a membership named literally 's_self' satisfied @Restricted(S_SELF) on ARBITRARY records — ownership was never compared. CoreTenantGuard already filtered system roles at its own call site; the decorator did not.",
      "file": "src/core/common/decorators/restricted.decorator.ts",
      "find": "    roles.filter((role) => !isSystemRole(role)),",
      "replace": "    roles,",
      "specs": [
        "tests/tenant-guard.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "aggregation-joins-cross-tenant",
      "defect": "11.35.0 — the tenant plugin prepended $match to the SOURCE collection only. $lookup / $unionWith / $graphLookup read a DIFFERENT collection whose own aggregate hook never fires, so a single reporting-shaped aggregation returned every tenant's rows from the joined collection. For tenant-scoped medical data that is the difference between one tenant's records and everyone's.",
      "file": "src/core/common/plugins/mongoose-tenant.plugin.ts",
      "find": "    secureCrossCollectionStages(pipeline, model);",
      "replace": "    void secureCrossCollectionStages;",
      "specs": [
        "tests/tenant-guard.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "cross-tenant-writes-accepted",
      "defect": "11.35.0 — the tenant plugin stamped tenantId only when absent, and filtered WHERE but not the update payload. So a caller-supplied tenantId survived (planting a row in a foreign tenant) and an update could rewrite tenantId (moving a row out of its own tenant). Both are cross-tenant writes from an ordinary request.",
      "file": "src/core/common/plugins/mongoose-tenant.plugin.ts",
      "find": "  if (value === undefined || value === null || value === ownTenantId) {\n    return;\n  }",
      "replace": "  return;\n  if (value === undefined || value === null || value === ownTenantId) {\n    return;\n  }",
      "specs": [
        "tests/tenant-isolation.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "upload-rejection-allow-list-not-http-exception",
      "defect": "11.32.4 §8 replaced multerFileFilter's bare-string rejection with `new Error(...)`, intending a clean 400. It never produced one: NestJS's `transformException` returns any NON-HttpException unchanged — its switch matches only multer's OWN message constants — so a file refused by the allow-list still reached the client as a 500, which tells the caller to retry a request that can never succeed. The test shipped with that fix asserted only that the callback receives an `Error`; it passed while the client got a 500, because it restated the implementation instead of the observable status.",
      "file": "src/core/common/helpers/file.helper.ts",
      "find": "cb(new BadRequestException(`File upload only supports the following filetypes",
      "replace": "cb(new Error(`File upload only supports the following filetypes",
      "specs": [
        "tests/unit/file-upload-rejection-status.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "upload-rejection-scriptable-not-http-exception",
      "defect": "The same defect on the scriptable-type rejection path added by 11.32.4 §6 (`text/html`, `image/svg+xml`). Registered as its own mutation rather than folded into the allow-list one: that branch is the more security-relevant of the two, and a single mutation cannot show the other is covered — fixing one and leaving the other was the concrete risk here.",
      "file": "src/core/common/helpers/file.helper.ts",
      "find": "return cb(new BadRequestException(`File upload rejected:",
      "replace": "return cb(new Error(`File upload rejected:",
      "specs": [
        "tests/unit/file-upload-rejection-status.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "file-access-audit-ignores-handler-roles",
      "defect": "11.35.x — the §16 boot warning read CONFIGURATION only. `applyFileRoles()` writes onto the BASE-class member, but decorator metadata lives on the function object, so a subclass that RE-DECLARES a download handler keeps its own @Roles() and that is the function Nest registers. A subclassed getFileById() with @Roles(S_EVERYONE) therefore served anonymous downloads while resolveRoles('downloadRoles', …) still answered [ADMIN] and nothing fired — silence in exactly the deployment the warning exists for. Two independent consumer projects shipped it. This mutation re-introduces the blind spot at its narrowest point: the audit stops reading the handler's own metadata and sees only the inherited class-level roles.",
      "file": "src/core/modules/file/core-file-access-audit.initializer.ts",
      "find": "const handlerRoles: string[] = Reflect.getMetadata('roles', fn as object) ?? [];",
      "replace": "const handlerRoles: string[] = [];",
      "specs": [
        "tests/unit/file-access-effective-roles.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "file-access-audit-skips-controllers",
      "defect": "Not a shipped defect — this proves the audit's WIRING is not vacuous. The subclass blind spot is only closed if DiscoveryService actually hands the initializer the registered file controller. If it returns nothing — provider never constructed, DiscoveryModule missing from CoreModule's imports, controllers absent from getControllers() for this application shape — every unit test still passes because they supply the classes through a stub, and the audit silently inspects nothing. A security check that quietly does not run is worse than none: its silence reads as a clean bill of health.",
      "file": "src/core/modules/file/core-file-access-audit.initializer.ts",
      "find": "    for (const wrapper of this.discoveryService?.getControllers() ?? []) {\n      consider(wrapper.instance);\n    }\n",
      "replace": "",
      "specs": [
        "tests/file-access-audit-wiring.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "betterauth-signin-input-unvalidated",
      "defect": "11.36.0 — the sign-in DTO carried @ApiProperty() only: documentation, no validation. Every line of the handler reads input.email, so a malformed body produced a TypeError and reached the client as a 500. On the most-probed endpoint any deployment has, that tells the caller to retry something that can never succeed and files their mistake in the same bucket as a real outage. This mutation strips the validators from the sign-in email so a present-but-wrong body is unguarded again.",
      "file": "src/core/modules/better-auth/core-better-auth.controller.ts",
      "find": "  @IsNotEmpty()\n  @IsEmail()\n  email: string;\n\n  @ApiProperty({ description: 'User password' })",
      "replace": "  email: string;\n\n  @ApiProperty({ description: 'User password' })",
      "specs": [
        "tests/unit/better-auth-signin-input-validation.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "betterauth-signin-missing-body-500",
      "defect": "11.36.0 — the OTHER half of the same defect, and the reason the validators alone are not enough. MapAndValidatePipe returns early on a falsy value (`if (!value || typeof value !== 'object' …) return value`), so a request with NO body is never validated: it reaches the handler as `undefined` and the first `input.email` read throws. Registered separately because each fix covers inputs the other cannot see, and a single mutation would not show that.",
      "file": "src/core/modules/better-auth/core-better-auth.controller.ts",
      "find": "    // a 400 \"Missing input\"; keep that contract.\n    if (!input) {\n      throw new BadRequestException('Missing input');\n    }",
      "replace": "    // a 400 \"Missing input\"; keep that contract.",
      "specs": [
        "tests/unit/better-auth-signin-input-validation.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "betterauth-revoke-sessions-not-wired",
      "defect": "Not a shipped defect — a NEW option, and this proves its wiring is not vacuous. `revokeSessionsOnPasswordReset` is only worth anything if it reaches `instance.options.emailAndPassword`, where Better-Auth reads it on its three reset routes. A field that never arrives would leave a password reset silently keeping the attacker's sessions alive while the configuration says the opposite — worse than not offering the option, because the operator believes they took the precaution.",
      "file": "src/core/modules/better-auth/better-auth.config.ts",
      "find": "revokeSessionsOnPasswordReset: config.emailAndPassword?.revokeSessionsOnPasswordReset === true,",
      "replace": "revokeSessionsOnPasswordReset: false,",
      "specs": [
        "tests/unit/better-auth-revoke-sessions-on-password-reset.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "betterauth-emailandpassword-shallow-merge",
      "defect": "11.36.1 — `config.options` was merged over the resolved Better-Auth config with a SHALLOW spread (only `advanced` was deep-merged). An `options.emailAndPassword` therefore did not add a key, it REPLACED the whole block — including the `password: { hash: nativeScryptHash, verify: nativeScryptVerify }` pair the framework installs. Better-Auth silently fell back to its own hasher, so every credential already in the database stopped verifying and every user of that deployment was locked out at once, at runtime and with nothing in the logs. The module's own README recommended exactly that shape for wiring `sendResetPassword`. Re-pointed in 11.38.0: the block now also chains `onPasswordReset`, so a shallow spread drops the legacy-store mirror as well as the password hasher — two credential-integrity guarantees, one spread.",
      "file": "src/core/modules/better-auth/better-auth.config.ts",
      "after": "    if (optionsEmailAndPassword && typeof optionsEmailAndPassword === 'object') {",
      "find": "      finalConfig.emailAndPassword = {\n        ...base,\n        ...override,\n        // Merged key-by-key rather than spread, because object spread copies an EXPLICITLY\n        // undefined value as a present key: `password: { hash: undefined }` would survive as\n        // `hash: undefined`, and Better-Auth resolves `password?.hash || hashPassword`, silently\n        // switching to its own hasher for WRITES while nest-server's scrypt verify still handles\n        // READS. The result is an asymmetric pair — anyone who resets their password can then\n        // never sign in again. An explicit override still wins; only `undefined` is ignored.\n        password: mergeDefined(base.password, override.password),\n        // `onPasswordReset` is subject to the SAME hazard as `password` above, and needs the\n        // opposite treatment: it must not be replaceable at all.\n        //\n        // It is what mirrors an applied IAM reset into the legacy bcrypt store. A project has\n        // an entirely reasonable motive to declare its own (audit logging, a notification\n        // mail) — and the spread would then drop the framework's, silently restoring the\n        // pre-11.38.0 defect where the OLD password stays valid on the legacy path after a\n        // reset, including a reset performed BECAUSE it leaked. Both stores report success.\n        //\n        // So the two are CHAINED rather than merged: the framework mirror always runs, the\n        // project hook runs after it. That is a deliberate asymmetry to `password`, where an\n        // explicit override wins — a hasher is a choice, a credential-store mirror is not.\n        ...chainOnPasswordReset(base.onPasswordReset, override.onPasswordReset),\n      };",
      "replace": "      void base;\n      finalConfig.emailAndPassword = { ...override };",
      "specs": [
        "tests/unit/better-auth-password-reset-wiring.spec.ts",
        "tests/unit/better-auth-revoke-sessions-on-password-reset.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "password-reset-template-requires-appname",
      "defect": "11.36.1 — `src/templates/password-reset.ejs` was rewritten into a branded HTML mail that references `<%= appName %>`. The legacy caller `UserService.sendPasswordResetMail()` (and the byte-identical call in nest-server-starter) passes only `{ link, name }`, so `ejs.compile(tpl)(data)` threw `ReferenceError: appName is not defined`, `POST /users/password/reset-request` answered HTTP 500, and password recovery stopped working for every consumer that does not ship its own copy of the template. A template is never imported, type-checked or linted, so nothing else in the toolchain could see it.",
      "file": "src/templates/password-reset.ejs",
      "find": "<h1>Hello <%= name %>,</h1>",
      "replace": "<h1><%= appName %> — Hello <%= name %>,</h1>",
      "specs": [
        "tests/unit/email-templates.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "session-cookie-must-not-carry-body-jwt",
      "defect": "With `cookies: { exposeTokenInBody: true }` the sign-in path converts the session token into a JWT for the response body (`resolveJwtToken`), which is what a bearer-using client needs. `processAuthResult()` then wrote that same JWT into the session cookie. Better-Auth resolves a session by the opaque token it stored, so the cookie pointed at nothing: the sign-in itself succeeded and every request afterwards was anonymous — `/iam/session` answered `success: false` for a session that plainly existed in the database, and a browser client was logged out again the moment it navigated. Invisible in the usual `local` baseline, where cookies-only mode makes the two values identical; it only bites in environments that set `exposeTokenInBody` (typically `e2e` / `ci`), which is exactly where nobody browses manually. Found from the outside: a consumer's whole Playwright suite passed locally (`lt dev test` runs NODE_ENV=local) and every authenticated spec failed in CI.",
      "file": "src/core/modules/better-auth/core-better-auth-cookie.helper.ts",
      "find": "    const cookieToken = sessionToken ?? result.token;",
      "replace": "    const cookieToken = result.token;",
      "specs": [
        "tests/unit/better-auth-cookie-helper.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "signin-cookie-drops-session-token",
      "defect": "11.36.2 — the sign-in call site stopped handing the opaque session token to processCookies(), so with `cookies.exposeTokenInBody` the cookie was written from the body's JWT again. Better-Auth resolves a session by the opaque token it stored, so `/iam/session` answered `success: false` for a session that existed in the database and a browser client was anonymous on the next navigation. The sibling mutation `session-cookie-must-not-carry-body-jwt` cannot catch this: `processCookies()` returns early once a session token is present, so `processAuthResult()` and every unit test of it are unreachable from the repaired path. Reachable only in local/development/ci/e2e — assertCookiesProductionSafe() refuses exposeTokenInBody in production and staging.",
      "file": "src/core/modules/better-auth/core-better-auth.controller.ts",
      "find": "        return this.processCookies(res, result, sessionToken);",
      "replace": "        return this.processCookies(res, result);",
      "specs": [
        "tests/stories/cookies-security-property.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "safe-test-db-pattern-unanchored",
      "defect": "SAFE_TEST_DB_PATTERN, the last guard before dropDatabase(), matched its markers as a substring ANYWHERE in the database name instead of as a delimited segment. `ci` hides inside soCIal / speCIal / finanCIal / priCIng / muniCIpal and `test` inside laTEST / conTEST / TESTimonials, so ordinary project databases read as disposable. tests/global-setup.ts drops whatever MONGODB_URI points at, and `lt dev up` exports it at the project's `<slug>-local` DEVELOPMENT database — so a developer on such a project who ran the e2e suite from that shell lost it, to the guard that exists to prevent exactly that. The guard had never had a test.",
      "file": "tests/db-lifecycle.reporter.ts",
      "find": "export const SAFE_TEST_DB_PATTERN = /(^|[-_])(e2e|ci|test|acctest)([-_]|$)/i;",
      "replace": "export const SAFE_TEST_DB_PATTERN = /(e2e|ci|test|acctest)/i;",
      "specs": [
        "tests/unit/db-lifecycle-guard.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "public-endpoint-drops-caller-identity",
      "defect": "11.36.5 — RolesGuard returned early on a public endpoint (@Roles(S_EVERYONE), or no @Roles) BEFORE authenticating, leaving `request.user` unset. In a legacy-JWT deployment there is no CoreBetterAuthMiddleware on forRoutes('(.*)'), so the guard was the only thing that could identify the caller: a signed-in user arrived anonymous at @CurrentUser(), serviceOptions.currentUser and securityCheck(). A public endpoint that personalises for signed-in callers degraded silently, with no error and no log. Reported on an upgrade from 11.1.13, where the S_EVERYONE branch was evaluated AFTER the user had been resolved.",
      "file": "src/core/modules/auth/guards/roles.guard.ts",
      "find": "      await this.tryIdentifyOptionalUser(context);\n",
      "replace": "",
      "specs": [
        "tests/public-endpoint-identity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "account-issuer-backfill-missing",
      "defect": "11.37.0 — better-auth 1.7 keys an account by (issuer, accountId) and sign-in filters on it verbatim (`account.issuer === createLocalAccountIssuer('credential')`). Every credential row written by 1.6 has no `issuer` field at all, so the comparison can never hold: the upgrade itself locked out EVERY existing password user, with a bare 401 and nothing in the log explaining it. The boot-time backfill closes that gap on the first start after the upgrade. Removing the call restores the lockout.",
      "file": "src/core/modules/better-auth/core-better-auth.service.ts",
      "after": "  async onModuleInit(): Promise<void> {",
      "find": "    await this.backfillAccountIssuers();",
      "replace": "",
      "specs": [
        "tests/unit/better-auth-account-issuer-backfill.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "account-issuer-missing-on-migrate",
      "defect": "11.37.0 — the legacy->IAM account migration wrote the credential account WITHOUT an `issuer`, so better-auth 1.7 could not find the row it had just created. The very sign-in that triggered the migration answered 401, and every later sign-in did too. Dropping the field restores that.",
      "file": "src/core/modules/better-auth/core-better-auth-user.mapper.ts",
      "after": "      await accountsCollection.insertOne({",
      "find": "        issuer: createLocalAccountIssuer('credential'),\n",
      "replace": "",
      "specs": [
        "tests/stories/bidirectional-auth-sync.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "account-issuer-missing-on-link-account",
      "defect": "11.37.0 — system setup linked the initial admin's credential account WITHOUT an `issuer`, so better-auth 1.7 could not resolve it. A fresh deployment created an admin who could never sign in — the one account that must work on a new installation.",
      "file": "src/core/modules/system-setup/core-system-setup.service.ts",
      "after": "      await context.internalAdapter.linkAccount({",
      "find": "        issuer: createLocalAccountIssuer('credential'),\n",
      "replace": "",
      "specs": [
        "tests/stories/system-setup.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "account-issuer-backfill-writes-nothing",
      "defect": "11.37.0 — the boot-time backfill is what lets a credential account written by better-auth 1.6 sign in after the upgrade. Narrowing its filter so it matches no row leaves every legacy account without an `issuer`, and better-auth 1.7 cannot resolve them: the user gets a bare 401. This mutation targets the backfill's WRITE, not its wiring — the e2e suite calls the method directly, so it can only observe the former.",
      "file": "src/core/modules/better-auth/core-better-auth.service.ts",
      "after": "      const pending = await accounts",
      "find": "        .find({ [issuerField]: { $exists: false }, providerId: 'credential' }, { projection: { _id: 1 } })",
      "replace": "        .find({ [issuerField]: { $exists: false }, providerId: '__never_matches__' }, { projection: { _id: 1 } })",
      "specs": [
        "tests/stories/better-auth-issuer-upgrade.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-skips-iam-sync-for-hashed-password",
      "defect": "11.38.0 — CoreUserService.resetPassword skipped the IAM password sync whenever the incoming value was already sha256, on the reasoning that IAM 'needs the plain password'. It does not: hashPasswordForBetterAuth runs its input through normalizePasswordForIam, which passes a 64-hex string through unchanged. The lt auth client hashes in the browser (ltSha256, seven call sites in nuxt-extensions), so the skipped branch was the ONLY branch a real frontend ever took: the legacy store got the new password, the IAM credential kept the old one, and the endpoint reported success. The plaintext case — the one the existing e2e test covered — worked throughout, which is why it survived.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "async resetPassword(token: string, newPassword: string",
      "find": "    const passwordForIamSync = newPassword;",
      "replace": "    const passwordForIamSync = /^[a-f0-9]{64}$/i.test(newPassword) ? undefined : newPassword;",
      "specs": [
        "tests/stories/password-reset-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "update-skips-iam-sync-for-hashed-password",
      "defect": "11.38.0 — the same 64-hex guard as reset-skips-iam-sync-for-hashed-password, on the OTHER password-writing path: CoreUserService.update(). Registered separately because the two are different entry points with different callers, and a fix applied to only one of them leaves a working password-reset next to a broken password-change.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "// Capture the submitted password for the IAM sync before super.update()",
      "find": "    const passwordForIamSync = (input as any).password;",
      "replace": "    const inputPassword = (input as any).password;\n    const passwordForIamSync = inputPassword && !/^[a-f0-9]{64}$/i.test(inputPassword) ? inputPassword : undefined;",
      "specs": [
        "tests/stories/password-reset-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "iam-reset-does-not-sync-to-legacy",
      "defect": "11.38.0 — nothing mirrored an IAM password reset back into the legacy bcrypt store. Better-Auth's onPasswordReset hook was never wired, so a deployment running Legacy Auth next to IAM kept the OLD password valid on the legacy path after a reset — including a reset performed BECAUSE the old password was compromised. Invisible through the sign-in APIs, because CoreAuthService.signIn delegates to IAM for any user carrying an iamId; it surfaces only once IAM is switched off, which is the one moment nobody is watching.",
      "file": "src/core/modules/better-auth/core-better-auth.module.ts",
      "after": "onPasswordReset: async ({ user }) => {",
      "find": "        const mirrored = await this.userMapperInstance?.syncPasswordToLegacy(user.id, user.email, normalizedPassword);",
      "replace": "        const mirrored = !!normalizedPassword && !!user.id;",
      "specs": [
        "tests/stories/password-reset-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "iam-reset-password-not-normalized",
      "defect": "11.38.0 — the native Better-Auth reset routes are forwarded rather than controller-handled, so nothing normalized their password while sign-in and sign-up both do. A plaintext reset was therefore stored as scrypt(plaintext) while every later sign-in checks scrypt(sha256(...)): the account was locked out with the password its owner had just chosen, one request after being told the reset succeeded.",
      "file": "src/core/modules/better-auth/core-better-auth-api.middleware.ts",
      "after": "protected normalizeResetPassword(req: Request, relativePath: string)",
      "find": "    req.body[route.field] = normalized;\n    return normalized;",
      "replace": "    return normalized;",
      "specs": [
        "tests/stories/password-reset-parity.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-endpoints-default-on",
      "defect": "11.38.0 — `auth.legacyEndpoints.enabled` defaulted to true, so a project that registered the legacy auth module and never made a decision kept a second, fully functional password-authentication surface open indefinitely: signIn/signUp/logout/refreshToken over GraphQL and /auth/* over REST, served by CoreAuthService rather than Better-Auth and therefore bypassing every IAM-side control (2FA, passkey, session revocation). The flip to false was the release's headline breaking change and was, when registered, observable by NOTHING: every environment in this repo's own config.env.ts opts in explicitly, and the only suites touching the option set `enabled: false`, the branch that behaved identically before and after. Reverting the fall-through left 1958 unit tests and the legacy-relevant e2e specs green — measured, not assumed.",
      "file": "src/core/modules/auth/helpers/legacy-endpoints.helper.ts",
      "after": "export function isLegacyEndpointEnabled(",
      "find": "  return legacyConfig?.enabled === true;",
      "replace": "  return legacyConfig?.enabled !== false;",
      "specs": [
        "tests/unit/legacy-endpoints-resolution.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "deprecation-initializer-blocks-boot",
      "defect": "11.38.0 — CoreLegacyAuthDeprecationInitializer awaited getMigrationStatus() inside onApplicationBootstrap. Nest waits for every bootstrap hook before listen() resolves, and that call is collection-scale work (six operations, including a guaranteed COLLSCAN and an aggregation materializing one object per migrated user). Every pod of every rolling deploy therefore paid for a log line, and a readiness probe could fail while the server counted users. Invisible to tsc: a voided promise and an awaited one type-check identically.",
      "file": "src/core/modules/auth/core-legacy-auth-deprecation.initializer.ts",
      "after": "onApplicationBootstrap(): void {",
      "find": "    void this.reportMigrationStatus();",
      "replace": "    return this.reportMigrationStatus() as unknown as void;",
      "specs": [
        "tests/unit/legacy-auth-deprecation-initializer.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-registry-leaks-across-requests",
      "defect": "11.38.0 — the in-flight reset password is carried from the API middleware to Better-Auth's onPasswordReset hook. Holding it in a module-level variable instead of an AsyncLocalStorage store makes two concurrent resets observe each other's value, which mirrors one user's password into another user's legacy account — a credential cross-contamination in which both requests succeed and nothing is logged. The registry states this safety property in its docstring; this mutation is what keeps the statement honest.",
      "file": "src/core/modules/better-auth/core-better-auth-password-reset.registry.ts",
      "after": "const storage = new AsyncLocalStorage<PasswordResetContext>();",
      "find": "export function runWithResetPassword<T>(normalizedPassword: string, fn: () => T): T {\n  return storage.run({ normalizedPassword }, fn);\n}\n\n/**\n * The normalized password of the reset currently being handled, or `undefined`\n * outside such a request.\n *\n * `undefined` means \"not a reset we saw the body of\" — never \"no password\". Callers\n * must skip the mirror rather than guess, because writing a wrong bcrypt hash would\n * lock the account out of the legacy path.\n *\n * @internal\n */\nexport function getInFlightResetPassword(): string | undefined {\n  const store = storage.getStore();\n  if (!store) {\n    return undefined;\n  }\n\n  // ONE-SHOT. The legacy mirror is the only legitimate reader and it reads once, so handing the\n  // value out a second time can only serve something that should not have it.\n  //\n  // Defence in depth rather than a fix for a known hole: an AsyncLocalStorage store is retained\n  // by any async resource created INSIDE the `run()` that outlives it — a timer registered during\n  // lazy plugin init, a cached promise. Such a resource would otherwise keep observing a\n  // password-equivalent from a request that finished long ago. Clearing on read bounds that to\n  // the moment the mirror actually runs.\n  const { normalizedPassword } = store;\n  store.normalizedPassword = undefined;\n  return normalizedPassword;\n}",
      "replace": "let sharedPassword: string | undefined;\n\nexport function runWithResetPassword<T>(normalizedPassword: string, fn: () => T): T {\n  sharedPassword = normalizedPassword;\n  return fn();\n}\n\nexport function getInFlightResetPassword(): string | undefined {\n  const value = sharedPassword;\n  sharedPassword = undefined;\n  return value;\n}",
      "specs": [
        "tests/unit/password-reset-registry.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-request-reveals-unknown-email",
      "defect": "11.38.0 — setPasswordResetTokenForEmail threw NotFoundException for an unknown address, so POST /users/password/reset-request answered 404 for unknown and 201 for known: a working oracle for 'does this person have an account here'. In a multi-tenant product that also answers who works at which customer. The framework already answered this correctly on the IAM path (Better-Auth returns the same body either way), so the two halves of one framework disagreed about the same question.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "async setPasswordResetTokenForEmail(",
      "find": "      if (!preventEnumeration) {\n        throw new NotFoundException(`No user found with email: ${email}`);\n      }",
      "replace": "      throw new NotFoundException(`No user found with email: ${email}`);",
      "specs": [
        "tests/stories/password-reset-enumeration.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-request-awaits-mail-send",
      "defect": "11.38.0 — the timing half of the account oracle. Equalising the status code is pointless while the response TIME still separates the two cases: the known path awaits a mail send (a network round trip to SMTP/Brevo, tens to hundreds of ms) and the unknown one returns immediately. A fix that changed only the status code would have given up the 'unknown address' hint in the UI and kept the oracle fully intact — strictly worse than before. Registered separately from reset-request-reveals-unknown-email because the two halves are independent: each turns a different assertion red.",
      "file": "src/server/modules/user/user.service.ts",
      "after": "async sendPasswordResetMail(",
      "find": "    // Deliberately NOT awaited — see the note above.\n    void this.emailService\n      .sendMail(created.user.email, 'Password reset', {",
      "replace": "    await this.emailService\n      .sendMail(created.user.email, 'Password reset', {",
      "specs": [
        "tests/stories/password-reset-enumeration.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "security-headers-disabled-by-default",
      "defect": "11.38.0 — the security headers must be ON for a project that configures nothing. Making them opt-in reproduces the state this feature was built for: projects across the stack rebuilding the same header set by hand, and a freshly generated project sending none at all. A missing header produces no error anywhere; it surfaces in a pentest report months later. The safe answer has to be the one you get without thinking about it.",
      "file": "src/core/common/middlewares/security-headers.middleware.ts",
      "after": "protected resolveConfig()",
      "find": "    const options: ISecurityHeaders = raw === true || raw === undefined ? {} : raw;",
      "replace": "    if (raw === undefined) {\n      return null;\n    }\n    const options: ISecurityHeaders = raw === true ? {} : raw;",
      "specs": [
        "tests/security-headers.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "security-headers-hsts-ignores-protocol",
      "defect": "11.38.0 — HSTS must be decided by the actual request protocol and never by configuration. A browser REMEMBERS Strict-Transport-Security: one sent from a dev server over http://localhost makes every other project on that host unreachable over http for up to a year, and nothing on the server side can withdraw it. Sending it regardless of protocol is therefore not a cosmetic bug but an unrecoverable one, and it is invisible in production (where the header is correct) — it only bites developers.",
      "file": "src/core/common/middlewares/security-headers.middleware.ts",
      "after": "use(req: Request, res: ExpressResponse, next: NextFunction): void {",
      "find": "    if (config.hsts && this.isSecureRequest(req)) {",
      "replace": "    if (config.hsts) {",
      "specs": [
        "tests/security-headers.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-request-body-reveals-unknown-email",
      "defect": "11.38.0 — the third layer of the same oracle, and the one that survived the first two fixes. With the service returning null for an unknown address and the status codes equalised, the resolver still answered `!!user`: `true` for a known address, `false` for an unknown one. Equal status, different body — the distinction simply moved one layer up, to the value the caller actually reads. Found by the story suite during development, which is why it exists.",
      "file": "src/server/modules/user/user.resolver.ts",
      "after": "async requestPasswordResetMail(",
      "find": "    await this.userService.sendPasswordResetMail(email);\n    return true;",
      "replace": "    return !!(await this.userService.sendPasswordResetMail(email));",
      "specs": [
        "tests/stories/password-reset-enumeration.e2e-spec.ts",
        "tests/server.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "password-reset-link-ignores-config",
      "defect": "11.38.0 — the password-reset mail linked to the API host, because Better-Auth's generated URL (https://api.example.com/iam/reset-password/<token>?callbackURL=...) was passed through into the template untouched. It works — the API redirects to the app — but it puts a domain the recipient does not recognise into a password mail, which is exactly what people are trained to check before clicking. Restoring the pass-through also silently ignores `betterAuth.emailVerification.passwordResetLink`, so a project that configured its reset page would still mail the API link with nothing indicating why.",
      "file": "src/core/modules/better-auth/core-better-auth-email-verification.service.ts",
      "after": "async sendPasswordResetEmail(",
      "find": "    const { user } = options;\n    const url = this.buildPasswordResetUrl(options);",
      "replace": "    const { url, user } = options;",
      "specs": [
        "tests/unit/password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-link-raw-appurl",
      "defect": "11.38.0 — `buildPasswordResetLink` read `appUrl` straight from the configuration instead of resolving it. The `local`/`ci`/`e2e` environments do not set the option (their localhost default lives in `resolveServerUrls`), so the read returned nothing and the method answered `null` in every local environment — including the framework's own e2e suite, where this was found. A host-split `baseUrl` such as `https://api.crm.localhost` from `lt dev up` was likewise never derived. Since 11.39.0 the resolution lives in the shared `resolveAppUrlFromConfig`, so this mutation reverts the CALL rather than the inlined block.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  buildPasswordResetLink(token: string): null | string {",
      "find": "    const appUrl = resolveAppUrlFromConfig(this.configService);",
      "replace": "    const appUrl = this.configService.getFastButReadOnly<string>('appUrl');",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-link-concatenates-undefined",
      "defect": "11.38.0 — `email.passwordResetLink` had no default and nothing validated it, and callers concatenated whatever it held with the token. A project that never set it mailed `…/auth/reset-password/undefined` to a real recipient. The worst shape a bug can take: the request succeeds, the mail arrives, it looks right, and only the click reveals it — to somebody who by definition has no second way in. Returning `null` lets the caller send nothing instead, which at least says \"try again\".",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  buildPasswordResetLink(token: string): null | string {",
      "find": "      if (!appUrl) {\n        this.userServiceLogger.error(\n          'Cannot build a password-reset link: neither `email.passwordResetLink` nor `appUrl` is configured',\n        );\n        return null;\n      }\n      target = `${appUrl.replace(/\\/$/, '')}/auth/reset-password?token={token}`;",
      "replace": "      target = `${appUrl}/auth/reset-password`;",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "password-reset-token-lost-in-process",
      "defect": "11.38.0 — the token was read back off the object `process()` returns, and the security interceptor strips `passwordResetToken` there (correctly: a reset token in a response body is one in a log, a proxy cache and a browser history). So the only method that could mint a token could not hand it over, and a project that asked for it got `undefined` and mailed it. Generating the token OUTSIDE `process()` is what makes it reachable without weakening the scrubbing.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  async createPasswordResetToken(",
      "find": "    return { token, user };",
      "replace": "    return { token: (user as unknown as { passwordResetToken?: string }).passwordResetToken as string, user };",
      "specs": [
        "tests/stories/password-reset-token-reachable.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-link-ignores-derive-optout",
      "defect": "11.38.0 — the app-URL resolution did not forward `cors.deriveAppUrl`, so the derivation ran even for a deployment that had switched it off. That flag is how a project states the apex domain is NOT its own — the documented case being a third-party-hosted marketing site — and `buildCorsConfig` honours it. Ignoring it mails a password-reset token to that origin, where it lands in somebody else's access log. Since 11.39.0 the resolution is shared, so a single omission here breaks BOTH reset flows at once — which is the point of sharing it: one decision, one place, one test surface.",
      "file": "src/core/common/helpers/cookies.helper.ts",
      "after": "export function resolveAppUrlFromConfig(configService: {",
      "find": "    deriveAppUrl: configService.getFastButReadOnly<boolean>('cors.deriveAppUrl'),\n",
      "replace": "",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts",
        "tests/unit/password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reference-impl-rebuilds-reset-link",
      "defect": "11.38.0 — the framework's OWN reference implementation (`src/server/modules/user/user.service.ts`) kept `setPasswordResetTokenForEmail` plus a hand-built link after `createPasswordResetToken` and `buildPasswordResetLink` existed to replace them. `email.passwordResetLink` is never set in this repo's config and the token is stripped by the security interceptor, so the mail said literally `undefined/undefined` — both production incidents reproduced verbatim in the repository that fixed them. It mattered beyond this repo because CLAUDE.md points projects at that file as the shape to copy and the CLI generates from the starter that mirrors it. Nothing caught it: every new test asserted a building block, none asserted the mail.",
      "file": "src/server/modules/user/user.service.ts",
      "after": "  async sendPasswordResetMail(email: string, serviceOptions?: ServiceOptions): Promise<null | User> {",
      "find": "          link,\n",
      "replace": "          link: `${this.configService.configFastButReadOnly.email.passwordResetLink}/${created.user.passwordResetToken}`,\n",
      "specs": [
        "tests/stories/password-reset-token-reachable.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-link-fallback-path-segment",
      "defect": "11.38.0 — the legacy fallback appended the token as a PATH segment while the reset page shipped by nuxt-base-starter (`app/pages/auth/reset-password.vue`) reads `route.query.token`. A project that configured nothing therefore got a link that reached the right page, which then reported an invalid token because it read an empty one. The fallback now carries a `{token}` placeholder in the query position, so an unconfigured project gets a working link against the frontend this stack actually ships. Configured values keep the path-segment rule — that half is what protects the projects whose page IS built for a path.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  buildPasswordResetLink(token: string): null | string {",
      "find": "/auth/reset-password?token={token}`;",
      "replace": "/auth/reset-password`;",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-link-convention-warning-disarmed",
      "defect": "11.38.0 — two link conventions live side by side, and which one applies depends on whether `email.passwordResetLink` is set at all: the same string yields `?token=` when omitted and a path segment when written out. The behaviour is kept deliberately (a project configuring such a value has a page built for it), so the boot warning is the ONLY thing that makes the divergence discoverable. Disarming it restores a silent mismatch whose symptom appears solely to whoever clicks the link in the mail.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  protected warnOnAmbiguousResetLinkConvention(): void {",
      "find": "    if (typeof configured !== 'string' || !configured.trim().length || configured.includes('{token}')) {\n      return;\n    }",
      "replace": "    return;",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "password-reset-link-ignores-redirectto",
      "defect": "11.38.0 — `buildPasswordResetUrl` resolved the app link purely from configuration and ignored the `redirectTo` the caller had sent, even though nuxt-base-starter sends it on every request and Better-Auth's own hop would have redirected to exactly that value. A project that renamed its reset page therefore received the framework's guess instead of its own page, and the recipient landed on a route that does not exist. Reading it back is safe only because Better-Auth runs originCheck on `redirectTo` at request time — see readCallbackUrlFromBetterAuthLink().",
      "file": "src/core/modules/better-auth/core-better-auth-email-verification.service.ts",
      "after": "  protected buildPasswordResetUrl(options: SendPasswordResetEmailOptions): string {",
      "find": "      target = this.readCallbackUrlFromBetterAuthLink(options.url);",
      "replace": "      target = undefined;",
      "specs": [
        "tests/unit/password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "better-auth-errors-not-wrapped",
      "defect": "11.38.0 — Better-Auth's error responses reached the client in Better-Auth's own vocabulary (`{ code: 'INVALID_TOKEN', message: 'Invalid token' }`), while the frontends in this stack translate by parsing nest-server's `#LTNS_XXXX:` marker and nothing else. The parser found no code and passed the raw English string through, so every IAM error was shown to the end user as developer text. It bit hardest on the password-reset page, where an expired link answers INVALID_TOKEN and the person who already cannot sign in is the one reading it.",
      "file": "src/core/modules/better-auth/core-better-auth-error-codes.helper.ts",
      "after": "export async function wrapBetterAuthErrorResponse(response: Response): Promise<Response> {",
      "find": "  if (response.ok) {\n    return response;\n  }",
      "replace": "  return response;",
      "specs": [
        "tests/unit/better-auth-error-codes.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-link-warning-omits-precondition",
      "defect": "11.38.0 — the boot warning told projects to add a `{token}` placeholder without saying that only `CoreUserService.buildPasswordResetLink()` substitutes it. Its audience is by definition the projects that have NOT switched yet — one of them followed the advice while still concatenating the link by hand, which would have mailed `/reset-password/{token}/<token>`: a link that arrives, looks right, and fails only for whoever clicks it. That is the exact failure this area was repaired for, re-created by the warning meant to prevent it. Advice correct for half its audience and silently harmful to the other half is worse than none.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  protected warnOnAmbiguousResetLinkConvention(): void {",
      "find": "        'to silence this warning. FIRST make sure the mail is built by ' +\n        'CoreUserService.buildPasswordResetLink(): it is the only thing that substitutes {token}, so a ' +\n        'sendPasswordResetMail() that still concatenates the link itself would mail the placeholder ' +\n        'verbatim (\"/reset-password/{token}/<token>\").',",
      "replace": "        'to silence this warning.',",
      "specs": [
        "tests/unit/legacy-password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "better-auth-expired-link-says-sign-in-again",
      "defect": "11.38.0 — `TOKEN_EXPIRED` was mapped to nest-server's session-oriented `TOKEN_EXPIRED` (\"please sign in again\"). Better-Auth throws that code in exactly one place: an expired verification LINK (email-verification.mjs:178), one line above the INVALID_TOKEN it throws for a broken one. So a single user action — clicking an old link in a mail — produced two opposite instructions, and the wrong one covered the MORE common half, since links expire far more often than they are mangled. At that point the user is typically not signed in at all, which makes \"sign in again\" a dead end rather than merely imprecise.",
      "file": "src/core/modules/better-auth/core-better-auth-error-codes.helper.ts",
      "after": "const BETTER_AUTH_ERROR_CODE_MAP: Readonly<Record<string, string>> = Object.freeze({",
      "find": "  TOKEN_EXPIRED: ErrorCode.LINK_INVALID_OR_EXPIRED,",
      "replace": "  TOKEN_EXPIRED: ErrorCode.TOKEN_EXPIRED,",
      "specs": [
        "tests/unit/better-auth-error-codes.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "verification-link-omits-email",
      "defect": "11.38.0 — the email-verification link carried only the token. That is sufficient while the token is valid and useless once it is not: the verification page shipped by nuxt-base-starter gates its \"send a new email\" button on `route.query.email`, so an expired link produced a correct explanation and no way to act on it. The page cannot recover the address itself — it lives in the token's JWT payload, and reading it there would mean rendering data from an unverified signature. The dead end was reached by exactly the people who needed the button.",
      "file": "src/core/modules/better-auth/core-better-auth-email-verification.service.ts",
      "after": "  protected buildFrontendVerificationUrl(token: string, email?: string): string {",
      "find": "    if (email) {\n      url += `&email=${encodeURIComponent(email)}`;\n    }",
      "replace": "",
      "specs": [
        "tests/unit/verification-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "smtp-secure-ignores-port",
      "defect": "11.38.0 — the production SMTP profile paired the default port 587 with `secure` resolving to true unless explicitly disabled. `secure` means implicit TLS, which only port 465 speaks; 587 opens in plaintext and upgrades via STARTTLS. Nodemailer therefore sent a TLS ClientHello to a server answering with an SMTP greeting and OpenSSL reported `wrong version number`. A deployment that configured only host and credentials could send NO mail at all — invisibly, because authentication mail is deliberately not awaited, so the API answered 200 while every password-reset message died in transport. Found on a production deployment, not by a test.",
      "file": "src/core/common/helpers/config.helper.ts",
      "after": "export function resolveSmtpSecure(value: string | undefined, port: number): boolean {",
      "find": "  // Unset or non-canonical: 465 is the only port that speaks implicit TLS.\n  return port === 465;",
      "replace": "  return normalized !== 'false';",
      "specs": [
        "tests/unit/smtp-tls-config.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-token-never-expires",
      "defect": "11.38.0 — a legacy password-reset token had no expiry at all: `resetPassword()` looked it up by value and compared no time, while the exception it threw read \"Invalid or expired password reset token\", claiming a check that did not exist. A reset link takes over an EXISTING account, so an unbounded lifetime means a mail in an archive, a forwarded message or a restored backup opens that account years later. Treating a MISSING timestamp as valid is the specific half that matters: every row written before this release has none, so reading it as \"valid forever\" would preserve the defect permanently rather than for a migration window.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  protected isPasswordResetTokenExpired(expiresAt: Date | null | undefined): boolean {",
      "find": "    if (!expiresAt) {\n      return true;\n    }",
      "replace": "    if (!expiresAt) {\n      return false;\n    }",
      "specs": [
        "tests/unit/password-reset-token-expiry.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "legacy-reset-expiry-invalid-opts-out",
      "defect": "11.38.0 — the expiry knob resolves `0` to \"never expires\" and ANY invalid value to the 60-minute default. The asymmetry is the protection: switching off the expiry of an account-takeover credential is a decision somebody has to state, and a typo, an empty variable or a negative number must never be the thing that states it. `Number('')` is `0`, so an empty environment variable would otherwise read as a deliberate opt-out.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  protected passwordResetTokenExpiryMinutes(): number {",
      "find": "    if (typeof value === 'number' && Number.isFinite(value) && value >= 0) {\n      return value;\n    }\n\n    return DEFAULT_PASSWORD_RESET_TOKEN_EXPIRY_MINUTES;",
      "replace": "    return typeof value === 'number' && Number.isFinite(value) && value >= 0 ? value : 0;",
      "specs": [
        "tests/unit/password-reset-token-expiry.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-mail-omits-deadline",
      "defect": "11.38.0 — the reset token gained an expiry (§11) and no mail template mentioned it. Until then there was nothing to announce, because the token never expired; afterwards the link dies after 60 minutes and the recipient learns that nowhere. Somebody who reads the mail in the evening, clicks, and is told the link is invalid cannot tell an expired link from a broken one — which is the same experience the whole area was repaired for: a mail arrives, looks right, and does not work. Found by the session that wrote the original fix, while applying the new expiry to its own project.",
      "file": "src/templates/password-reset-de.ejs",
      "after": "<html lang=\"de\">",
      "find": "              <% if (expiryMinutes) { %>",
      "replace": "              <% if (false) { %>",
      "specs": [
        "tests/unit/email-templates.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "reset-password-accepts-nosql-operator",
      "defect": "11.38.1 — `@Body('token') token: string` has `metatype === String`, and MapAndValidatePipe returns basic-type values verbatim (`isBasicType(metatype)` short-circuit), so the declared type is erased at runtime and a JSON object reaches the service. `findOne({ passwordResetToken: { $ne: null } })` then selected the first user holding ANY live reset token and set that account's password — full takeover, with the attacker never seeing the mail. Confirmed by probe against the real route before the guard existed. Express parses query strings with qs in extended mode, so `?token[$ne]=` reaches the sibling verification sinks identically.",
      "file": "src/core/modules/user/core-user.service.ts",
      "after": "  async resetPassword(token: string, newPassword: string, serviceOptions?: ServiceOptions): Promise<TUser> {",
      "find": "    if (!isQueryableString(token)) {\n      throw new NotFoundException(ErrorCode.LINK_INVALID_OR_EXPIRED);\n    }\n\n",
      "replace": "",
      "specs": [
        "tests/stories/password-reset-token-reachable.e2e-spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "production-smtp-secure-unwired",
      "defect": "11.38.0 — the production profile paired port 587 with `secure` defaulting to true, a combination that cannot connect, so a deployment configuring only host and credentials sent no mail at all. The helper that fixes it has its own coverage; the WIRING in config.env.ts had none, so reverting this one line restored the outage verbatim and left the whole suite green. That is the same 'block-level tests green, mail broken' shape the rest of this release exists to end.",
      "file": "src/config.env.ts",
      "after": "  production: {",
      "find": "        secure: resolveSmtpSecure(process.env.SMTP_SECURE, productionSmtpPort),",
      "replace": "        secure: process.env.SMTP_SECURE !== 'false',",
      "specs": [
        "tests/unit/smtp-production-wiring.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "production-smtp-no-require-tls",
      "defect": "11.38.1 — repairing the SMTP transport made the channel usable for the first time, and `secure: false` alone means OPPORTUNISTIC STARTTLS: nodemailer upgrades only when the server advertises it (smtp-connection/index.js:1506), and `requireTLS` is opt-in. An on-path attacker stripping that capability line gets a plaintext session carrying the SMTP credentials and a working password-reset link. Before the repair nothing was sent at all, so this is a new exposure introduced by the fix rather than a pre-existing one.",
      "file": "src/config.env.ts",
      "after": "  production: {",
      "find": "        requireTLS: process.env.SMTP_REQUIRE_TLS !== 'false',\n",
      "replace": "",
      "specs": [
        "tests/unit/smtp-production-wiring.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "error-wrapper-collapses-set-cookie",
      "defect": "11.38.1 — the header rebuild in `wrapBetterAuthErrorResponse` used `Headers.forEach` + `set()`. `forEach` yields each `set-cookie` entry separately and `set()` overwrites, so all but the last were discarded. Better-Auth CLEARS session and 2FA cookies on several failure paths — precisely the responses this function rewrites — so the clear was lost or partial and a stale credential stayed in the browser. `sendWebResponse` already used `getSetCookie()` for this exact reason; the new helper reintroduced the hazard upstream of that care.",
      "file": "src/core/modules/better-auth/core-better-auth-error-codes.helper.ts",
      "after": "export async function wrapBetterAuthErrorResponse(response: Response): Promise<Response> {",
      "find": "    for (const cookie of response.headers.getSetCookie?.() ?? []) {\n      headers.append('set-cookie', cookie);\n    }\n\n",
      "replace": "",
      "specs": [
        "tests/unit/better-auth-error-codes.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "iam-reset-link-raw-appurl",
      "defect": "11.39.0 — `buildPasswordResetUrl` read `appUrl` straight off the configuration while its legacy twin had already been moved to the shared resolver. Three behaviours were missing as a result: the localhost default that `local`/`ci`/`e2e` rely on (none of them set `appUrl`), the derivation from a host-split `baseUrl` such as `https://api.crm.localhost` that `lt dev up` serves, and the `cors.deriveAppUrl` opt-out that keeps a reset token out of an untrusted apex domain. The registry asserted the raw read was a defect on the legacy side while tolerating it here — two hand-maintained copies of one decision, drifting.",
      "file": "src/core/modules/better-auth/core-better-auth-email-verification.service.ts",
      "after": "  protected buildPasswordResetUrl(options: SendPasswordResetEmailOptions): string {",
      "find": "    const appUrl = resolveAppUrlFromConfig(this.configService);",
      "replace": "    const appUrl = this.configService.getFastButReadOnly<string>('appUrl');",
      "specs": [
        "tests/unit/password-reset-link.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "ai-allowlist-string-fails-open",
      "defect": "11.39.x — `ai.allowedBaseUrlHosts` is reachable through the framework's own `NSC__AI__ALLOWED_BASE_URL_HOSTS` mapping, which delivers a STRING. `getEnvironmentObject()` plus lodash `merge` assigns that scalar straight over the configured array, and the old `!Array.isArray(...) -> return` read it as \"no allowlist configured\" and skipped the SSRF egress check entirely. An operator using the canonical `NSC__` spelling silently disabled egress control, with no log line and no error.",
      "file": "src/core/modules/ai/providers/openai-compatible.provider.ts",
      "after": "protected resolveAllowedBaseUrlHosts(): string[] {",
      "find": "    const entries = typeof configured === 'string' ? configured.split(',') : configured;",
      "replace": "    const entries = configured;",
      "specs": [
        "tests/unit/ai-egress-allowlist.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "ai-allowlist-case-sensitive",
      "defect": "11.39.x — `URL.host` / `URL.hostname` are always lowercase, so an allowlist entry written `LLM.Example.com` in a .env would never match. It fails CLOSED, so it is not an egress hole — but the only symptom is a WARN plus \"the AI stopped working\", which is a slow and confusing incident for a purely cosmetic difference.",
      "file": "src/core/modules/ai/providers/openai-compatible.provider.ts",
      "after": "protected normaliseHostEntry(value: string): string {",
      "find": "      .toLowerCase()\n      .replace(/\\.(?=$|:)/, '');",
      "replace": "      .replace(/\\.(?=$|:)/, '');",
      "specs": [
        "tests/unit/ai-egress-allowlist.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "ai-allowlist-probe-unguarded",
      "defect": "11.39.x — the provider has three outbound `fetch` paths and `probeContextWindow()` consulted the allowlist on none of them. It is not admin-only: `CoreAiService` calls `detectAndPersistCapabilities()` on an ordinary user prompt whenever `contextWindow` is undefined, and it runs BEFORE `checkRateLimit()`. The original spec drove `assertBaseUrlAllowed` directly, so it proved the rule and could not see that a caller skipped it.",
      "file": "src/core/modules/ai/providers/openai-compatible.provider.ts",
      "after": "protected async probeContextWindow(): Promise<number | undefined> {",
      "find": "    this.assertBaseUrlAllowed(`${base}/api/show`);\n    const response = await fetch(`${base}/api/show`, {",
      "replace": "    const response = await fetch(`${base}/api/show`, {",
      "specs": [
        "tests/unit/ai-egress-allowlist.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-overrides-range-key-blind",
      "defect": "11.39.x — `targetPackage()` split an override key on `>` to strip the `parent>child` scope, but `>` is also the version operator. `\"ws@>=8.0.0 <8.21.0\"` resolved to `\"=8.0.0 <8.21.0\"`, so no advisory ever matched such a key: the TOO LOW and NOT MATCHING classes were unreachable for 10 of this repo's 17 overrides, while the run still closed with `ok — 17 override(s) checked ... none is failing` and printed 10 false UNUSED warnings. One-sided keys (`pkg@<X`) were unaffected, which is why the original suite — bare and one-sided keys only — stayed green.",
      "file": "scripts/check-overrides.mjs",
      "after": "function targetPackage(key) {",
      "find": "    .split(/(?<![@<>=\\s])>/)",
      "replace": "    .split('>')",
      "specs": [
        "tests/unit/check-overrides.guard.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-overrides-ignores-workspace-yaml",
      "defect": "11.39.x — pnpm 11 no longer reads the `pnpm` field in package.json and expects `overrides` / `auditConfig` in pnpm-workspace.yaml. A guard reading only package.json reports \"no overrides declared\" for every pnpm 11 workspace — finding nothing and reporting it as the all-clear, which is the exact failure the script exists to catch. All lenne.tech base repos are on pnpm 11 with their overrides in the YAML, so this is the normal case, not an edge one.",
      "file": "scripts/check-overrides.mjs",
      "after": "const overrides = {",
      "find": "  ...workspaceMap(['overrides']),",
      "replace": "",
      "specs": [
        "tests/unit/check-overrides.guard.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "graceful-shutdown-rejection-unreported",
      "defect": "11.39.x — the test guarding this was `await expect(Promise.resolve()).resolves.toBeUndefined()`, which is true no matter what the helper does. Verified by sabotage: with the `.catch` removed so a failing close vanished silently, the old suite stayed green at 7/7. It also never awaited the helper's `.catch` chain, so the log landed after the test — occasionally after the whole FILE, during worker teardown, surfacing as `EnvironmentTeardownError: Closing rpc while \"onUserConsoleLog\" was pending` blamed on whichever spec ran last.",
      "file": "src/core/common/helpers/graceful-shutdown.helper.ts",
      "after": "timer = setTimeout(() => {",
      "find": "\n        .catch((error) => logger.error(`Shutdown failed: ${error instanceof Error ? error.message : 'Unknown error'}`));",
      "replace": ";",
      "specs": [
        "tests/unit/graceful-shutdown.helper.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "graceful-shutdown-cap-silent",
      "defect": "11.39.x — an implausible `shutdownDelayMs` is capped so a typo ends in a late shutdown rather than a SIGKILL, but the cap is only half the behaviour: an operator who never hears about it keeps the typo, and the next orchestrator with a shorter grace period kills the process mid-wait. The test asserted only that the close happened at the capped time, not that anything was reported.",
      "file": "src/core/common/helpers/graceful-shutdown.helper.ts",
      "after": "if (delayMs > MAX_SHUTDOWN_DELAY_MS) {",
      "find": "    logger.warn(\n      `shutdownDelayMs is ${delayMs}ms, which exceeds the ${MAX_SHUTDOWN_DELAY_MS}ms cap and is longer than a typical ` +\n        `orchestrator grace period — capping it, since a longer wait only ends in SIGKILL.`,\n    );",
      "replace": "",
      "specs": [
        "tests/unit/graceful-shutdown.helper.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-counts-assume-advisories",
      "defect": "11.39.x — the audit counts were derived from `parsed.advisories` unconditionally. npm 7+ emits `auditReportVersion: 2` with a `vulnerabilities` map and NO `advisories` key at all, so every severity came out 0 while the whole raw tally was filed under \"ignored\": a real, unassessed CRITICAL rendered as `critical 0 (1 ignored)`. That is the exact confusion the assessed/ignored split exists to prevent, produced in reverse — and the branch shipped with no test at all, because `runAudit` was not exported.",
      "file": "scripts/check.mjs",
      "after": "export function splitAuditCounts(parsed) {",
      "find": "  if (raw && parsed?.advisories) {\n    const advisories = Object.values(parsed.advisories);",
      "replace": "  if (raw) {\n    const advisories = Object.values(parsed.advisories ?? {});",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-degrade-retired-only",
      "defect": "11.39.x — the degraded-audit path matched ONLY the retired legacy endpoint signature. When the WORKING bulk advisory endpoint failed transiently (registry itself answering 200 in 0.24s, `/-/npm/v1/security/advisories/bulk` timing out after 20s), pnpm emitted its own error envelope `{\"error\":{\"code\":23,\"message\":\"The operation was aborted due to timeout\"}}` and `check` hard-failed at its FIRST step. It hit nest-server, nest-server-starter and nuxt-extensions inside one hour. That is the exact \"paints every check red until somebody stops reading a red audit\" hazard the surrounding comment argues against, produced by the guard written to prevent it.",
      "file": "scripts/check.mjs",
      "after": "export function isAuditEndpointUnavailable(out) {",
      "find": "  let envelope;\n  try {\n    envelope = JSON.parse(out.slice(out.indexOf('{')))?.error;\n  } catch {\n    envelope = undefined;\n  }",
      "replace": "  let envelope;",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-degrades-auth-refusal",
      "defect": "11.39.x — the degraded-audit signature matched `\\b5\\d\\d\\b` anywhere in pnpm's error envelope, including a status QUOTED by a proxy that refused the request itself (`403 Forbidden: upstream returned 503`). A wrong token therefore degraded to \"infrastructure, not blocking\": the audit silently stopped running for good and nobody was told why. The plain 401/403 cases could not catch it — they never reach the signature at all, so they stayed green with the guard deleted.",
      "file": "scripts/check.mjs",
      "after": "export function isAuditEndpointUnavailable(out) {",
      "find": "    if (\n      /\\b(4\\d\\d)\\b/.test(String(envelope.code ?? '')) ||\n      /\\bunauthor|\\bforbidden\\b|\\bauthentication\\b/i.test(String(envelope.message ?? ''))\n    ) {\n      return false;\n    }\n",
      "replace": "",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-ambiguity-blind",
      "defect": "11.39.x — with the advisory service unreachable, pnpm can exit 0 with `advisories: {}` and every count at zero, byte-identical to a genuinely clean repo and carrying no error envelope. The degraded path keys on a non-zero exit plus a signature, so it was never consulted: the run printed a green `✓ audit  critical 0 · high 0 · …` and PASSED, locally and in CI, having verified nothing. A hang is loud and a timeout is catchable; this asserts SAFETY that was never established.",
      "file": "scripts/check.mjs",
      "after": "export function isAuditResultAmbiguous(parsed) {",
      "find": "  return SEVERITIES.every((severity) => !raw[severity]);",
      "replace": "  return false;",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-hang-unrecognised",
      "defect": "11.39.x — `pnpm audit` against an unreachable registry does not fail, it HANGS: no output, no exit, no error envelope. Measured by lt-monorepo-00 at >240s and zero bytes. Every degraded signature needs the process to have said something, so none could fire, and the audit step had no time limit at all (the idle watchdog is scoped to test steps because build/typecheck/audit legitimately buffer their output). `check` then waited forever — strictly worse than a red run, because a failure tells you something and a hang tells you nothing while holding the terminal.",
      "file": "scripts/check.mjs",
      "after": "export function isAuditEndpointUnavailable(out) {",
      "find": "  if (/AUDIT_STEP_HUNG/.test(out)) {\n    return 'hung';\n  }\n",
      "replace": "",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-zero-exit-no-tally",
      "defect": "11.39.x — an audit that exited 0 while emitting nothing the gate could parse printed a GREEN tick with a literal `0` beside it, having assessed nothing. `counts` was null so the all-zero ambiguity probe was never entered (it needs a parsed report), and the degradation expression only considered a NON-zero exit, so `degradedReason` stayed false. The Vulnerabilities section said 'counts unavailable' while the step line said the audit passed. Sibling of the silent-outage case one step further out: no report at all rather than an empty one, and the probe cannot rescue it — an answering service says nothing about a tally that was never parsed.",
      "file": "scripts/check.mjs",
      "after": "export function resolveAuditDegradation({ code, counts, out, silentOutage }) {",
      "find": "    return code !== 0 ? isAuditEndpointUnavailable(out) : 'unreadable';",
      "replace": "    return isAuditEndpointUnavailable(out);",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "jsonc-stripper-eats-globs",
      "defect": "11.39.x — readJsonc() stripped block comments with a regex that cannot tell a comment from a string literal, and every config it reads is full of globs that look like one. `src/**/*.spec.ts` collapsed to `src*.spec.ts` (the `/**/` IS a complete comment pair) and a bare `src/**` opened a comment running to the next `*/` anywhere later in the file. Measured: 2783 bytes gone from tsconfig.tests.json, and .oxlintrc.json lost an entire overrides entry (4 -> 3). Both files still PARSED, so all four assertions in the suite ran green against a mutilated config for their whole life.",
      "file": "tests/unit/toolchain-contract.spec.ts",
      "after": "const stripJsonComments = (raw: string): string => {",
      "find": "  let out = '';\n  let inString = false;",
      "replace": "  return raw.replace(/^\\s*\\/\\/.*$/gm, '').replace(/\\/\\*[\\s\\S]*?\\*\\//g, '');\n  let out = '';\n  let inString = false;",
      "specs": [
        "tests/unit/toolchain-contract.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-audit-5xx-from-free-text",
      "defect": "11.40.0 — the 5xx signature was matched with `\\b5\\d\\d\\b` against `code + message` joined, so any number 500-599 anywhere in the envelope degraded the run. pnpm's own progress prose supplies one (`audited 503 packages`), turning a run that had to BLOCK into a yellow 'not blocking' warning. Found by lt-monorepo-00 by testing the signature list against cases that must NOT degrade.",
      "file": "scripts/check.mjs",
      "after": "export function isAuditEndpointUnavailable(out) {",
      "find": "      /\\btimeout\\b|\\baborted\\b|ETIMEDOUT|ECONNRESET|ENOTFOUND|EAI_AGAIN/i.test(message) ||",
      "replace": "      /\\btimeout\\b|\\baborted\\b|ETIMEDOUT|ECONNRESET|ENOTFOUND|EAI_AGAIN|\\b5\\d\\d\\b/i.test(message) ||",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "check-probe-ignores-registry",
      "defect": "11.40.0 — the reachability probe asked registry.npmjs.org unconditionally while pnpm audits against the CONFIGURED registry. Behind a private registry or proxy: pnpm fails silently against its own registry, the probe asks npmjs.org, npmjs.org answers, the run reports 'no outage' and prints a green tick. The exact false-green the probe exists to prevent, one layer down. Found by measuring three real outage modes (502, dead DNS, connection refused), all of which make pnpm audit exit 0 with a complete all-zero report and NO error envelope.",
      "file": "scripts/check.mjs",
      "after": "function advisoryBulkUrl(registry) {",
      "find": "  let base = typeof registry === 'string' ? registry.trim() : '';",
      "replace": "  let base = '';",
      "specs": [
        "tests/unit/check-script-metrics.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "shared-registry-block-drift",
      "defect": "11.40.0 — the registry-resolution block is duplicated in scripts/check.mjs and scripts/check-overrides.mjs because the guard is copied ALONE into a temp dir by its own spec (an import broke 40 cases with ERR_MODULE_NOT_FOUND). Duplication without a drift check means one copy can silently keep asking the wrong host while both runs stay green — which IS the defect being fixed, since both probes originally hardcoded registry.npmjs.org while pnpm audits against the configured registry.",
      "file": "scripts/check-overrides.mjs",
      "after": "// >>> SHARED-WITH-CHECK-MJS",
      "find": "  const fallback = 'https://registry.npmjs.org/';",
      "replace": "  const fallback = 'https://registry.example.invalid/';",
      "specs": [
        "tests/unit/shared-registry-resolution.spec.ts"
      ],
      "driverSpecific": false
    },
    {
      "id": "sentence-splitter-quadratic",
      "defect": "The sentence splitter must match the TERMINATOR only. A leading `[^.!?]+` is quadratic: it eats to end-of-string, backtracks a character at a time when the terminator fails, and matchAll restarts the walk one position along. That form took 6.7 SECONDS on a 100 KB description — of BLOCKED EVENT LOOP, on every prompt build — and tool descriptions are not all first-party: CoreAiMcpClientService wraps whatever a remote MCP server advertises, uncapped. The linearity test claimed to pin this and nothing proved it did; verified 2026-09-04 at ratio 16.6 against a threshold of 8.",
      "file": "src/core/modules/ai/services/core-ai-prompt-builder.service.ts",
      "after": "The pattern matches the TERMINATOR ONLY, via a lookahead.",
      "find": "for (const match of text.matchAll(/[.!?]+(?=\\s|$)/g)) {",
      "replace": "for (const match of text.matchAll(/[^.!?]+[.!?]+(?=\\s|$)/g)) {",
      "specs": [
        "tests/unit/ai.spec.ts"
      ],
      "driverSpecific": false
    }
  ]
}
